// import { combineShares, splitSecret } from './shamir'

describe('split and combine', () => {
  test('shouldsplitandcombineshares', async () => {
    // const testSecret = 'this is a test secret'
    // const shares = splitSecret(testSecret, 2, 3)
    // expect(shares.length).toBe(3)
    // expect(combineShares([shares[0], shares[1]])).toBe(testSecret)
    // expect(combineShares([shares[1], shares[2]])).toBe(testSecret)
    // expect(combineShares([shares[0], shares[2]])).toBe(testSecret)
    // expect(combineShares([shares[0], shares[1], shares[2]])).toBe(testSecret)
    expect(1).toBe(1)
  })
})
